// numbers in javascript

// variable of number datatype
const num = 500.123;

const otherNum = new Number(200);

console.log(otherNum);

let number = 1.1214;

// converting number into exponent
let n = num.toExponential(3);
console.log(n);

// we can convert number to string and can use the properties of strings
// converting number into string
let v = num.toString().length;
console.log(v);

console.log(num.toFixed(2));
