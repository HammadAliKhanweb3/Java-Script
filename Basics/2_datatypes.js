"use strict ";    //tereats all js code as newer version
 alert(5+5)   // we are using in node not browser so it will throw error
 // string
 let Name="Hammad"     
// number
 let Age=19             
// Boolean
 let isLoggedin=true    
// we can also declare variable with keyword but it is not good practice
 userId=51              
 console.log(userID);
 
 
 /* Prem
 number => 2 to power 53 (no of data can store)
 Bigint  => (for heavy data )
 string =>  {we write string in double coutation ("")}
 Boolean =>  {Boolean will give only true/false}
 null    =>  standaloneValue
 undefined 
 symbol  =>  unique
 
 
 */
